[package]
name = "realm"
version = "0.1.0"
edition = "2024"

[dependencies]
uuid = { version = "1.20.0", features = ["v4"] }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
macroquad = "0.4.14"

[dev-dependencies]
tempfile = "3.10"
